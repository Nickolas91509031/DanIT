const inputLabel = {
    label: document.querySelector(".password"),
    value: label.value,
};

const userData = {
    userPassword: "123123"
};

function getPasswords(value) {
    for (let i = 0; i <= 100000000; i++) {
        value = i;
    }
}
